# Frontend được phục vụ CÙNG ORIGIN với backend (qua Dev Tunnel) -> dùng đường dẫn tương đối.
# Nhờ vậy link tunnel đổi thế nào cũng chạy, không cần build lại.
VITE_API_URL=/api
VITE_SOCKET_URL=/

# (Cũ - khi deploy tách Render/Vercel)
# VITE_API_URL=https://moji-realtimechatapp-backend.onrender.com/api
# VITE_SOCKET_URL=https://moji-realtimechatapp-backend.onrender.com/
